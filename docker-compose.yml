version: "3"

services:
  backend:
    container_name: koors_backend
    build:
      context: ./back
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - web
      - internal
    environment:
      - PORT=4000
    volumes:
      - ./back/persist:/app/persist
      - ./back/persist-save:/app/persist-save

  nginx:
    container_name: nginx_koors
    image: nginx
    restart: unless-stopped
    networks:
      - web
      - internal
    volumes:
      - ./build:/etc/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
    labels:
      - traefik.enable=true
      - traefik.backend=nginx_koors
      - traefik.docker.network=web
      # Port
      - traefik.http.services.nginx_koors.loadBalancer.server.port=80

      # Rule
      - traefik.http.routers.nginx_koors.rule=Host(`koors-test.jehanno.net`)
      # SSL configuration
      - traefik.http.routers.nginx_koors.entryPoints=websecure
      - traefik.http.routers.nginx_koors.tls=true
      - traefik.http.routers.nginx_koors.tls.certResolver=myresolver

networks:
  web:
    external: true
  internal:
    driver: bridge
